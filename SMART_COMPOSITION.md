# 一鍵智慧構圖

相機底部的「智慧構圖」按鈕只在使用者點擊時執行一次，不會持續分析相機影格。

流程：

1. 使用既有 Photo Output 擷取一張不會儲存到相簿的暫存照片。
2. 透過裝置端 ML Kit 偵測人臉。
3. 將人臉範圍交給純 TypeScript 推薦器。
4. 顯示推薦名稱與原因。
5. 使用者按下「套用構圖」後才切換模板與人物輪廓。

目前規則：

- 單人偏左：留白構圖
- 單人偏右：右側三分法
- 單人大臉占比：半身近距離人像
- 單人置中：人物置中
- 雙人：雙人置中
- 三人以上：多人置中

所有位置判斷都先換算成相片的 normalized 座標概念，不依賴特定手機解析度。分析影像不會上傳，也不會進入修圖頁或相簿。

驗證狀態：TypeScript、ESLint、單元測試與 Android Debug build 已通過。尚未在實機驗證前後鏡頭、方向、人臉數量、弱光與分析時間；Windows 無法執行 iOS build。
