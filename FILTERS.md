# 即時濾鏡

底部「濾鏡」選單提供 18 款即時濾鏡，分為基本、人物、美食、風景、夜景與風格：

- 基本：原圖、自然、暖色、冷色
- 人物：清透、柔霧、蜜桃、日系
- 美食：鮮味、咖啡
- 風景：晴空、森林、夕陽
- 夜景：霓虹、藍調
- 風格：黑白、復古、電影

強度範圍 0–100%，每次增減 10%；重設回到原圖。

預覽使用 Vision Camera Skia 的 frame renderer，儲存時對相機照片套用相同的色彩矩陣。
## 拍照後修圖

拍照後先進入編輯畫面，不會立即寫入相簿。編輯功能包括：

- 分類選擇濾鏡與調整強度
- 曝光、對比、飽和度與色溫
- 按住比較原圖
- 全部重設、重拍與儲存

編輯狀態只保存參數，按下儲存時才從拍攝照片進行一次全解析度 Skia 渲染並輸出 JPEG，避免滑動過程反覆壓縮。照片先透過 `Photo.toImageAsync()` 套用正確方向；構圖輪廓、引導線和控制面板不會寫入照片。

目前輸出不保留原始 EXIF metadata，也不另存未編輯原圖。取消或重拍會放棄尚未儲存的編輯結果。

驗證：TypeScript、ESLint、10 個單元測試與 Android `assembleDebug` 通過。裝置上的前後鏡頭、直橫拍、預覽與成品色彩一致性、全尺寸輸出時間及長時間效能仍需驗收。
Windows 環境無法執行 iOS build。
